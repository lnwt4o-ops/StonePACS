<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="StonePACS.Views.AuthWindow"
        Title="Admin Authentication"
        Width="300" Height="180"
        WindowStartupLocation="CenterOwner"
        CanResize="False" SystemDecorations="BorderOnly">

    <Border BorderBrush="#007ACC" BorderThickness="2" Background="White" Padding="20">
        <StackPanel Spacing="15">
            <TextBlock Text="🔒 Admin Access Required" FontWeight="Bold" Foreground="#007ACC" HorizontalAlignment="Center"/>
            
            <TextBox Name="UserBox" Watermark="Username" />
            <TextBox Name="PassBox" Watermark="Password" PasswordChar="●" />
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10">
                <Button Content="Cancel" Click="Cancel_Click" Classes="Small"/>
                <Button Content="Login" Click="Login_Click" Classes="Small Primary" IsDefault="True"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>