<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:StonePACS.ViewModels"
        x:Class="StonePACS.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="Stone PACS" WindowStartupLocation="CenterScreen"
        Width="1024" Height="768">

    <Grid ColumnDefinitions="240, *">
        
        <Border Grid.Column="0" Background="#2C3E50">
            <StackPanel Spacing="5" Margin="10">
                <TextBlock Text="STONE PACS" 
                           Foreground="White" FontWeight="Bold" FontSize="22" 
                           HorizontalAlignment="Center" Margin="0,30,0,40"/>
                
                <Button Command="{Binding GoToWorklistCommand}" 
                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                        Background="Transparent" Foreground="White" Padding="15,12" CornerRadius="8">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="📋"/>
                        <TextBlock Text="Worklist" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding GoToRegistrationCommand}" 
                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                        Background="Transparent" Foreground="White" Padding="15,12" CornerRadius="8">
                     <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="➕"/>
                        <TextBlock Text="New Registration" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding GoToExamCodeSetupCommand}" 
                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                        Background="Transparent" Foreground="White" Padding="15,12" CornerRadius="8">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="⚙️"/>
                        <TextBlock Text="Exam Code Setup" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="#F4F5F7">
            <ContentControl Content="{Binding CurrentView}" Margin="0"/>
        </Border>
        
    </Grid>
</Window>